#!/usr/bin/env bash

# Find the PID of the running 4-to_infinity_and_beyond process and kill it
pid=$(pgrep -f "4-to_infinity_and_beyond")

# Check if the process was found
if [ -n "$pid" ]; then
    kill "$pid"
    echo "Terminated"
else
    echo "Process not found"
fi
